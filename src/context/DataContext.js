// the create Context function is built into React and allows us to share properties with components in a broader sense, this imported thorughout various components to simplify that process//

import {createContext} from 'react'

export const DataContext = createContext([])